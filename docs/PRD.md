# PRD：演讲内容智能 Repurposing 平台

## 1. 文档信息

| 项目 | 内容 |
|:---|:---|
| 文档版本 | v0.2 |
| 撰写日期 | 2026/06/22 |
| 产品名称 | 待定（内部代号：SpeechRepurposer） |
| 目标读者 | 产品经理、技术负责人、设计师、左总 |
| 状态 | Draft |

---

## 2. 产品概述

### 2.1 一句话描述

把一场演讲的原始素材（视频、音频、文字稿、幻灯片、照片）自动转化为适合多平台传播的短视频、社媒文案、金句卡和多语言版本，并保持演讲者本人的口吻与风格。

### 2.2 愿景

让思想领袖、专家学者、企业高管能够**零剪辑基础**地将自己的演讲内容，一键变成可在抖音、视频号、小红书、LinkedIn、X/Twitter 上分发的高质量社媒资产。

### 2.3 核心价值主张

| 痛点 | 解决方案 |
|:---|:---|
| 演讲内容很珍贵，但传播形态单一 | 自动拆解成多个短视频片段 |
| 人工剪辑短视频成本高、周期长 | AI 自动策划脚本 + 自动渲染 |
| 多语言、多平台适配耗时 | 一键生成多语言版本与多平台版式 |
| 代运营写不出本人风格 | 基于过往材料学习演讲者口吻 |
| 不知道哪段内容适合传播 | AI 按传播潜力评分并推荐 |

---

## 3. 背景与问题

### 3.1 市场背景

- 知识型内容在短视频平台流量持续上升
- 学术会议、行业峰会、企业内训产生大量优质长内容
- 但这些内容通常只在现场或单次直播中被消费，长尾价值未释放

### 3.2 用户痛点

1. **剪辑门槛高**：教授/专家不会用剪辑软件，助理剪辑耗时
2. **内容提炼难**：不知道 1 小时演讲里哪 30 秒值得剪
3. **风格一致性差**：代运营写的文案不像本人
4. **多语言成本高**：想把内容出海，但配音翻译成本贵
5. **平台适配繁琐**：同一内容要手动改尺寸、改字幕样式、改文案长度

### 3.3 竞品分析

#### OpusClip

**定位**：长视频 → 批量短视频片段，面向播客主、网红、营销团队。

**核心 UX 流程**：

```
上传视频 → 配置参数 → AI 处理生成片段 → 按 Virality Score 排序 → 审校预览 → 导出/发布
```

**值得借鉴的功能**：

| 功能 | 说明 | 借鉴价值 |
|:---|:---|:---|
| **AI 精选片段** | 自动提取 5-10 个 viral 潜力片段 | 高，我们同样需要自动切片 |
| **Virality Score™** | 0-100 分预测爆款潜力 | 高，可改为"传播潜力分" |
| **自动转竖屏** | 9:16 带人物追踪 | 中，P1 加 |
| **动态字幕** | 关键词高亮、emoji | 中，P0 可简化 |
| **AI Hook** | 自动生成前 3 秒标题 | 高，P0 必做 |
| **自然语言筛选** | "找出讨论定价策略的片段" | 中，P1 加 |

**局限性**：20-40% 的 AI 片段需要丢弃；对复杂叙事、学术内容理解浅；创意控制力弱。

#### Descript / Underlord

**定位**：文本驱动型音视频编辑器 + AI 协作者，面向播客、访谈、知识型内容。

**核心 UX 流程**：

```
上传音视频 → 自动转录文字稿 → 自然语言下指令 → AI 执行多步编辑 → 审校初稿 → 文本精修
```

**值得借鉴的功能**：

| 功能 | 说明 | 借鉴价值 |
|:---|:---|:---|
| **文本编辑视频** | 改文字稿 = 改视频 | 高，P1 做字幕级编辑 |
| **Underlord AI 助手** | 自然语言执行多步编辑任务 | 中，我们可类似做"重新生成" |
| **语气词/停顿清理** | 一键删除 um/uh/长停顿 | 低，非核心需求 |
| **Show Notes / 章节标记** | 自动生成节目描述 | 中，可生成 LinkedIn 长帖 |
| **多语言翻译配音** | 30+ 语言带口型同步 | 中，P2 做 |
| **Overdub** | 文字合成本人声音 | 低，P2 考虑 |

**局限性**：复杂多轨剪辑仍需专业软件；创意类编辑（"让这更有能量感"）需要人工；生成的社交文案偏泛。

#### InVideo

**定位**：提示词/文本 → 完整视频，面向社媒运营、营销人员、小型团队。

**核心 UX 流程**：

```
输入提示词 → AI 生成脚本/素材/配音/字幕 → 审校编辑器替换素材 → 导出
```

**值得借鉴的功能**：

| 功能 | 说明 | 借鉴价值 |
|:---|:---|:---|
| **品牌套件** | 统一字体/颜色/logo | 中，P1 做字幕样式模板 |
| **多语言一键生成** | 100+ 语言 | 中，P2 参考这种一键感 |
| **音色选择** | 多个 AI 配音可选 | 中，P1 通用 TTS 阶段可用 |
| **素材替换界面** | 左侧片段缩略图 + 右侧预览 | 高，P0 审校页参考 |
| **提示词引导** | 自然语言控制生成风格 | 中，生成时可加"更学术"等指令 |

**局限性**：从零生成视频，不基于已有演讲内容；AI 脚本偏泛；创意控制力弱。

#### 与我们的差异

| 维度 | OpusClip | Descript | InVideo | 我们的项目 |
|:---|:---|:---|:---|:---|
| 目标用户 | 网红/播客主 | 播客/访谈 | 营销/社媒运营 | 教授/专家/会议主办方 |
| 起点 | 已有长视频 | 已有音视频 | 一句话提示词 | 已有演讲素材 |
| 核心差异点 | 批量 viral 片段 | 文本编辑音视频 | 从零生成视频 | 读懂 Speaker + 保持口吻 + 多素材输入 |

**结论**：我们借鉴它们的**审校流程、评分机制、文本编辑体验、品牌套件**，但核心卖点是**"像本人"和"多素材输入"**。

---

## 4. 目标用户与用户画像

### 4.1 主要用户

| 用户类型 | 特征 | 使用场景 |
|:---|:---|:---|
| **高校教授/研究员** | 学术演讲多，希望扩大影响力 | 会议演讲后自动生成传播素材 |
| **企业高管/行业专家** | 有观点输出需求，时间碎片化 | 峰会演讲后生成 LinkedIn 内容 |
| **会议主办方** | 有大量演讲视频资产 | 批量生成大会宣传短片 |
| **知识型 KOL** | 内容产出频繁 | 把长直播/课程切成短视频 |
| **专家助理/运营团队** | 实际执行者 | 上传素材、审校 AI 输出、发布 |

### 4.2 用户画像示例

**张教授**
- 55 岁，某高校 AI 研究院院长
- 经常参加国际会议，演讲 30-60 分钟
- 不会剪辑，但希望学生/助理能把他的演讲发到社媒
- 有 Twitter/LinkedIn，但更新不频繁
- 希望内容保持学术严谨，同时让非专业读者能看懂

---

## 5. 产品范围

### 5.1 In Scope（本期）

- Speaker Profile 创建与管理
- 项目级演讲素材上传（视频、音频、文字稿、幻灯片、图片）
- 演讲者过往材料上传（书籍、文章、旧演讲、社媒内容）
- 口吻设定与声音样本管理
- AI 自动分析：人物风格画像、内容切片、传播潜力评分
- AI 生成：
  - 3-5 个竖屏精彩片段（脚本 + 字幕 + 画面建议）
  - 抓眼球标题 / Hook
  - 多语言翻译版本
  - LinkedIn 长帖
  - 金句卡 / Carousel
- 人工审校界面
- 导出视频、图片、文案

### 5.2 Out of Scope（本期）

- 直接发布到社媒平台（只导出，不接入平台 API）
- 实时直播处理
- AI 生成虚拟人形象
- 复杂的团队协作权限（先做单用户）
- 付费订阅与计费系统

---

## 6. 用户故事

### 6.1 上传与创建

**US-001**：作为用户，我希望创建一位 Speaker 的资料，这样系统能记住他的风格。

**US-002**：作为用户，我希望上传一场演讲的视频或音频，让系统自动转写成文字。

**US-003**：作为用户，我希望上传文字稿或幻灯片，作为 AI 分析的素材。

**US-004**：作为用户，我希望上传演讲者过去的文章或书籍章节，帮助 AI 理解他的风格。

**US-005**：作为用户，我希望上传一段声音样本，用于生成与本人相似的配音。

### 6.2 生成

**US-006**：作为用户，我希望系统自动提取演讲中的金句和高光片段。

**US-007**：作为用户，我希望系统根据演讲者风格生成短视频脚本。

**US-008**：作为用户，我希望系统生成适合 LinkedIn 发布的长帖文案。

**US-009**：作为用户，我希望系统生成适合多平台的金句图片。

**US-010**：作为用户，我希望系统把内容翻译成英文或其他语言。

### 6.3 审校与导出

**US-011**：作为用户，我希望预览 AI 生成的视频和文案。

**US-012**：作为用户，我希望编辑生成的脚本、字幕和标题。

**US-013**：作为用户，我希望替换视频中的画面或 BGM。

**US-014**：作为用户，我希望一键导出所有生成内容。

---

## 7. 功能需求

### 7.1 Speaker Profile 管理

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-001 | 创建 Speaker | P0 | 姓名、身份、头像、语言偏好 |
| FR-002 | 编辑 Speaker 信息 | P0 | 修改基础资料 |
| FR-003 | 查看 Speaker 风格画像 | P0 | AI 生成的人物风格摘要，可人工编辑 |
| FR-004 | 管理 Speaker 的过往材料 | P0 | 增删改书籍、文章、社媒内容 |
| FR-005 | 管理 Speaker 声音样本 | P1 | 上传/删除/预览克隆效果 |

### 7.2 项目（演讲）管理

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-006 | 创建项目 | P0 | 关联一个 Speaker，输入演讲标题/事件名称 |
| FR-007 | 查看项目列表 | P0 | 按 Speaker/状态筛选 |
| FR-008 | 删除项目 | P1 | 软删除 |
| FR-009 | 项目状态跟踪 | P0 | 上传中/处理中/待审校/已完成 |

### 7.3 素材上传

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-010 | 上传视频 | P0 | 支持常见格式，大小上限待定 |
| FR-011 | 上传音频 | P0 | 支持常见格式 |
| FR-012 | 上传文字稿 | P0 | 支持 docx/txt/md，或粘贴文本 |
| FR-013 | 上传幻灯片 | P0 | 支持 pdf/ppt/pptx |
| FR-014 | 上传现场照片 | P0 | 支持 jpg/png，可多选 |
| FR-015 | 上传演讲者过往材料 | P0 | 书籍、文章、旧演讲稿、社媒内容 |
| FR-016 | 素材预处理状态显示 | P0 | 转写中/解析中/已完成 |

### 7.4 人物理解与风格画像

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-017 | 自动生成 Speaker 风格画像 | P0 | 基于过往材料提取价值观、常用比喻、句式风格 |
| FR-018 | 人工编辑风格画像 | P0 | 用户可修改、补充、禁用某些表达 |
| FR-019 | 内容切片与评分 | P0 | 把演讲切成可传播片段并评分 |
| FR-020 | 关键帧/幻灯片页推荐 | P1 | 推荐适合作为画面的素材 |

### 7.5 口吻设定

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-021 | 风格滑块 | P0 | 学术严谨 ↔ 通俗口语；理性克制 ↔ 激情澎湃；简短直接 ↔ 详细展开 |
| FR-022 | 目标受众选择 | P0 | 同行学者/行业从业者/普通大众/投资人 |
| FR-023 | 口头禅/固定开场结尾 | P1 | 用户可设置常用表达 |
| FR-024 | 禁用词设置 | P2 | 避免某些词汇出现 |

### 7.6 声音样本

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-025 | 上传声音样本 | P1 | 1-3 分钟清晰录音 |
| FR-026 | 预览克隆声音 | P1 | 用样本文本试听 |
| FR-027 | 选择是否用于生成 | P1 | 用户可开关克隆声音 |
| FR-028 | 多语言音色映射 | P2 | 中文用本人克隆，英文可选英式/美式 |

### 7.7 内容生成

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-029 | 生成竖屏精彩片段 | P0 | 默认 3-5 个，用户可选数量 |
| FR-030 | 生成标题/Hook | P0 | 每个 clip 配 3 个备选标题 |
| FR-031 | 生成 LinkedIn 长帖 | P0 | 1 篇，符合 Speaker 口吻 |
| FR-032 | 生成金句卡 | P0 | 3-5 张 |
| FR-033 | 生成 Carousel | P1 | 多页图文，适合 LinkedIn/小红书 |
| FR-034 | 生成多语言版本 | P1 | 中英为主，其他语言后续扩展 |
| FR-035 | 生成画面/B-roll 提示 | P1 | 为每个 clip 标注建议画面 |

### 7.8 审校与编辑

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-036 | 脚本编辑 | P0 | 修改字幕、hook、结尾 |
| FR-037 | 标题选择/编辑 | P0 | 从 AI 备选标题中选或重写 |
| FR-038 | 画面替换 | P1 | 从上传素材中选其他画面 |
| FR-039 | BGM 替换 | P1 | 按情绪标签切换背景音乐 |
| FR-040 | 重新生成 | P0 | 对单个 clip 重新生成 |

### 7.9 导出

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| FR-041 | 导出视频 | P0 | MP4，竖屏 9:16 |
| FR-042 | 导出字幕文件 | P1 | SRT/VTT |
| FR-043 | 导出文案 | P0 | Markdown/TXT |
| FR-044 | 导出金句卡图片 | P0 | PNG/JPG |
| FR-045 | 批量导出 | P1 | 打包下载一个项目的所有产出 |

---

## 8. 非功能需求

### 8.1 性能

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| NFR-001 | 上传响应 | P0 | 素材上传后即时反馈，大文件分片 |
| NFR-002 | 生成耗时 | P0 | 简单项目（文字稿+图片）5 分钟内出初稿 |
| NFR-003 | 视频渲染 | P1 | 单个 clip 渲染 2 分钟内 |
| NFR-004 | 并发处理 | P1 | 支持多个项目排队异步处理 |

### 8.2 安全与隐私

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| NFR-005 | 用户数据隔离 | P0 | 不同用户数据不可互相访问 |
| NFR-006 | 素材安全存储 | P0 | 上传文件加密存储 |
| NFR-007 | 声音样本授权 | P1 | 明确告知用户声音样本仅用于本人内容生成 |
| NFR-008 | 版权合规提示 | P1 | 对音乐、图片等素材给出版权提醒 |

### 8.3 可用性

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| NFR-009 | 上传引导 | P0 | 明确告诉用户"传一种即可，多传更好" |
| NFR-010 | 状态透明 | P0 | 每个处理步骤显示进度 |
| NFR-011 | 审校友好 | P0 | 生成内容并排展示，便于对比 |
| NFR-012 | 移动端适配 | P2 | 上传大文件建议在桌面端 |

### 8.4 可扩展性

| ID | 需求 | 优先级 | 说明 |
|:---|:---|:---|:---|
| NFR-013 | 模块解耦 | P0 | 媒体处理、智能生成、渲染层可独立替换 |
| NFR-014 | 多语言扩展 | P1 | 新增语言不改动核心逻辑 |
| NFR-015 | 输出格式扩展 | P1 | 新增平台版式不改动核心逻辑 |

---

## 9. 技术架构

### 9.1 抽象架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│              （Web 界面 / 移动端界面 / 管理后台）              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   应用接口层：FastAPI                         │
│         （RESTful API、上传接口、任务调度、权限管理）           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                  核心智能层：MiniMax M3                       │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │ 长上下文理解 │ │ 多模态理解   │ │ 结构化内容生成       │   │
│  │             │ │ （图文视频） │ │ （JSON 脚本/文案）   │   │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │ 人物风格画像 │ │ 内容切片评分 │ │ 多语言翻译           │   │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      异步任务调度层                          │
│              （任务队列 + 后台工作节点）                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      媒体处理层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 语音识别  │ │ 视频抽帧  │ │ 文档解析  │ │ 图片处理  │      │
│  │ 引擎     │ │ 引擎     │ │ 引擎     │ │ 引擎     │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 声音克隆  │ │ 语音合成  │ │ 视频渲染  │ │ 图形生成  │      │
│  │ 服务     │ │ 服务     │ │ 引擎     │ │ 引擎     │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
│  ┌──────────┐                                              │
│  │ 配乐/音效 │                                              │
│  │ 资源服务  │                                              │
│  └──────────┘                                              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据持久化层                            │
│  ┌────────────┐ ┌────────────┐ ┌──────────────────────┐   │
│  │ 关系型数据库 │ │ 对象存储    │ │ 向量数据库（可选）    │   │
│  └────────────┘ └────────────┘ └──────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 架构说明

| 层级 | 已确定技术 | 职责 |
|:---|:---|:---|
| 客户端层 | **TanStack Start** | 用户上传素材、配置口吻、审校生成结果、导出内容 |
| 应用接口层 | **FastAPI** | 接收上传、管理 Speaker/项目/任务、对外暴露 REST API、协调各层 |
| 核心智能层 | **MiniMax M3** | 理解全部输入素材、生成人物画像、提取金句、生成脚本与社媒文案、多语言翻译 |
| 异步任务调度层 | **asyncio + 后台任务** | 把耗时任务（渲染）异步执行，P0 不上 Celery/Redis |
| 媒体处理层 | 待定 | 处理音视频的转写/抽帧/渲染，文档解析，图片处理，声音克隆与合成，配乐匹配 |
| 数据持久化层 | **PostgreSQL + 本地文件系统** | PostgreSQL 存元数据，本地文件系统存上传文件和生成结果 |

### 9.3 数据流

```
用户上传素材
    ↓
FastAPI 接收 → 存入本地文件系统 → 写入 PostgreSQL
    ↓
asyncio 后台任务分发
    ├── 现场照片 → 图片处理 → 压缩
    └── 幻灯片/PDF → 文档解析 → 图片 + 文本（P0 可选）
    ↓
MiniMax M3 统一理解：
    演讲内容 + 过往材料 + 现场图片 + 口吻设定
    ↓
MiniMax M3 输出结构化方案：
    clip 脚本 / LinkedIn 文案 / 金句卡 / 多语言版本
    ↓
asyncio 后台任务分发渲染
    ├── 视频渲染引擎 → 竖屏短片
    ├── 图形生成引擎 → 金句卡
    └── 配乐资源服务 → BGM
    ↓
FastAPI 返回结果 → TanStack Start 前端审校/下载
```

### 9.4 Agent 工作流设计

P0 必须内置 Agent 流程，核心原因是**用户对 AI 生成结果必然存在不满意的情况**，系统需要支持"生成 → 自评 → 修正 → 人工反馈 → 局部重生成"的闭环。

#### 核心 Agent 列表

| Agent | 职责 | 触发时机 |
|:---|:---|:---|
| Persona Agent | 基于过往材料生成 Speaker 风格画像 | Speaker 创建/更新后 |
| Content Analyzer | 分析演讲内容，切片、评分、提取金句 | 素材预处理完成后 |
| Hook Agent | 写抓眼球的开头 | 生成 clip 时 |
| Script Agent | 生成竖屏脚本 | 生成 clip 时 |
| LinkedIn Agent | 生成 LinkedIn 长帖 | 生成衍生内容时 |
| Quote Card Agent | 生成金句卡文案 | 生成衍生内容时 |
| Review Agent | 按 Speaker 风格评分并指出问题 | 每次生成后 |
| Reviser Agent | 根据反馈修改指定内容 | 自评未通过或用户反馈后 |
| Translator Agent | 多语言翻译 | 生成多语言版本时 |

#### 生成流程

```
上传素材并预处理
    ↓
Persona Agent → Speaker 风格画像
    ↓
Content Analyzer → 内容切片 + 传播潜力评分
    ↓
对每个高潜片段：
    Script Agent → 初稿脚本
    Review Agent → 风格评分
    如果 score < 7：
        Reviser Agent → 修正
    重复最多 2 次
    ↓
LinkedIn Agent / Quote Card Agent → 衍生内容
    ↓
等待用户审校
    ↓
用户反馈 → Reviser Agent → 局部重生成
```

#### 人工反馈循环（HITL）

用户反馈需要结构化，系统才能有效处理：

| 反馈类型 | 处理范围 |
|:---|:---|
| Hook 不吸引人 | 只重新生成 hook |
| 整体风格不像本人 | 重新生成整个脚本，可能需调整 persona/tone |
| 信息太复杂/太简单 | 按受众重新生成 |
| 事实不准确 | 提示用户检查源素材，不自动修正 |
| 想换表达方式 | 基于用户编辑内容再优化 |

---

## 10. 数据模型

### 10.1 用户（User）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| email | string | 登录邮箱 |
| name | string | 用户名 |
| created_at | datetime | 创建时间 |

### 10.2 演讲者（Speaker）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| user_id | UUID | 关联用户 |
| name | string | 姓名 |
| title | string | 身份/头衔 |
| avatar_url | string | 头像 |
| language | string | 主要语言 |
| persona | JSON | AI 生成的风格画像 |
| tone_settings | JSON | 口吻滑块设置 |
| created_at | datetime | 创建时间 |

### 10.3 项目（Project）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| speaker_id | UUID | 关联演讲者 |
| title | string | 项目标题 |
| event_name | string | 事件名称 |
| language | string | 内容语言 |
| status | enum | draft/uploading/processing/review/completed |
| tone_snapshot | JSON | 生成时的口吻设置快照 |
| created_at | datetime | 创建时间 |

### 10.4 素材（Asset）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| project_id | UUID | 关联项目 |
| type | enum | video/audio/transcript/slides/image/voice_sample/past_material |
| file_url | string | 文件地址 |
| transcript | text | 转录文本（音视频） |
| extracted_text | text | 提取文本（文档） |
| keyframes | JSON | 关键帧列表（视频） |
| slide_pages | JSON | 幻灯片页列表 |
| processed_at | datetime | 处理完成时间 |

### 10.5 生成片段（Clip）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| project_id | UUID | 关联项目 |
| hook | string | 开头 hook |
| script | JSON | 分镜脚本 |
| title_options | JSON | 备选标题 |
| subtitles | JSON | 字幕时间轴 |
| music_mood | string | 配乐情绪 |
| status | enum | generated/rendering/completed |
| video_url | string | 生成视频地址 |
| duration | int | 时长（秒） |
| language | string | 语言 |

### 10.6 衍生内容（Derivative）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | UUID | 主键 |
| project_id | UUID | 关联项目 |
| type | enum | linkedin_post/quote_card/carousel/multilingual_script |
| content | text/JSON | 内容 |
| language | string | 语言 |
| image_url | string | 图片地址（金句卡） |
| status | enum | generated/edited |

---

## 11. 核心用户流程

### 11.1 创建 Speaker 并上传素材

```
1. 用户登录
2. 创建 Speaker Profile
   ├── 输入姓名、身份、语言
   └── 可选上传头像
3. 上传演讲者过往材料
   ├── 书籍/文章/旧演讲稿
   ├── 社媒内容（文本或链接）
   └── 声音样本（可选）
4. 系统基于过往材料生成 Speaker 风格画像
5. 用户确认或编辑风格画像
```

### 11.2 创建项目并生成内容

```
1. 用户选择 Speaker
2. 创建新项目，输入演讲标题/事件名
3. 上传本场演讲素材（任选一种或多种）
   ├── 视频/音频
   ├── 文字稿
   ├── 幻灯片
   └── 现场照片
4. 设置口吻参数
   ├── 学术严谨 ↔ 通俗口语
   ├── 理性克制 ↔ 激情澎湃
   ├── 简短直接 ↔ 详细展开
   └── 目标受众
5. 选择输出类型
   ├── 竖屏精彩片段数量
   ├── 是否生成多语言
   ├── 是否生成 LinkedIn 长帖
   └── 是否生成金句卡
6. 系统异步处理
7. 用户进入审校页面
```

### 11.3 审校与导出

```
1. 用户查看生成的 clips 列表
2. 播放预览每个 clip
3. 编辑脚本、字幕、标题
4. 替换画面或 BGM
5. 查看 LinkedIn 长帖和金句卡
6. 确认后导出
   ├── 视频 MP4
   ├── 字幕文件
   ├── 文案文本
   └── 金句卡图片
```

---

## 12. 输出规格

### 12.1 竖屏精彩片段

| 属性 | 规格 |
|:---|:---|
| 画幅 | 9:16 |
| 分辨率 | 1080 × 1920 |
| 时长 | 15-60 秒，默认 30 秒 |
| 字幕 | 每行不超过 12 字，底部居中 |
| 画面 | 演讲者特写 / 现场照片 / 幻灯片页 / 动态文字 |
| 配乐 | 按情绪标签匹配 |

### 12.2 Hook / 标题

- 每个 clip 提供 3 个备选标题
- 前 3 秒字幕必须有冲突、反常识或强数据
- 标题长度适合各平台展示

### 12.3 LinkedIn 长帖

- 长度：150-300 英文词 / 300-600 中文字
- 结构：Hook → 核心观点 → 个人洞察 → 行动号召
- 口吻：保持 Speaker 风格，偏专业但可读

### 12.4 金句卡

- 画幅：1:1 或 4:5
- 内容：一句金句 + 署名
- 风格：3-5 套模板可选（科技/学术/极简/深色）
- 输出：PNG / JPG

### 12.5 Carousel

- 页数：5-8 页
- 第一页：标题 + 视觉
- 中间页：核心论点 / 金句
- 最后一页：CTA + 署名

### 12.6 多语言版本

- 首期：中文 → 英文
- 后续扩展：西/法/德/日/阿
- 翻译保留原意和冲击力
- 配音可选目标语言音色或 Speaker 克隆音色

---

## 13. API 概要（仅示意）

### 13.1 Speaker 管理

```
POST   /speakers
GET    /speakers
GET    /speakers/{id}
PUT    /speakers/{id}
DELETE /speakers/{id}
```

### 13.2 项目管理

```
POST   /projects
GET    /projects
GET    /projects/{id}
PUT    /projects/{id}
DELETE /projects/{id}
```

### 13.3 素材上传

```
POST /projects/{id}/assets
GET  /projects/{id}/assets
DELETE /projects/{id}/assets/{asset_id}
```

### 13.4 生成任务

```
POST /projects/{id}/generate          # 触发生成任务
GET  /projects/{id}/generate/status   # 查询生成状态
POST /projects/{id}/clips/{id}/regenerate
```

### 13.5 审校与导出

```
PUT  /clips/{id}                      # 编辑 clip
PUT  /derivatives/{id}                # 编辑衍生内容
POST /projects/{id}/export            # 批量导出
```

---

## 14. UI/UX 要求

### 14.1 上传页

- 清晰的素材类型分区
- 拖拽上传 + 点击上传
- 显示每个素材的处理状态
- "至少传一种，多传效果更好"的提示

### 14.2 Speaker 风格画像页

- 左侧：AI 生成的画像
- 右侧：用户可编辑的字段
- 提供"重新分析"按钮

### 14.3 生成结果页

参考 OpusClip 的"左侧列表 + 右侧预览"布局：

- **左侧 clip 列表**：显示传播潜力分、时长、当前状态
- **右侧预览区**：视频预览 + 字幕时间轴 + 编辑区
- **顶部 tab**：视频 / LinkedIn / 金句卡 / 多语言
- **操作按钮**：导出、重新生成、删除
- **排序/筛选**：按分数、时长、状态筛选

### 14.4 审校页

参考 Descript 的文本编辑体验：

- **脚本编辑器**：按时间轴展示字幕，每句可独立编辑
- **字幕即视频**：修改字幕文案后，视频字幕同步更新
- **画面替换**：缩略图网格，从上传素材中选择
- **BGM 替换**：按情绪分类（沉稳/激昂/悬疑/希望）
- **标题选择**：A/B/C 三个备选 + 自定义
- **反馈按钮**：对 clip 整体、hook、字幕、风格分别提交不满意原因

### 14.5 从竞品借鉴的 UX 要点

| 借鉴点 | 来源 | P0 是否采纳 | 说明 |
|:---|:---|:---|:---|
| 上传即处理，无需复杂配置 | OpusClip | ✅ | 用户上传后自动进入处理流程 |
| AI 生成 + 评分排序 | OpusClip | ✅ | 每个 clip 显示传播潜力分 |
| 左侧列表 + 右侧预览 | OpusClip | ✅ | 标准审校布局 |
| Hook/标题 A/B 测试 | OpusClip | ✅ | 每个 clip 提供 3 个备选标题 |
| 自然语言筛选片段 | OpusClip | ❌ P1 | "找出讨论 X 的片段" |
| 文本编辑视频 | Descript | ❌ P1 | 改字幕 = 改视频，P0 只做字幕编辑 |
| AI 多步编辑指令 | Descript | ❌ P1 | 类似 "把这一集打磨成 YouTube 版" |
| 多语言翻译配音 | Descript | ❌ P2 | 后续扩展 |

---

## 15. 成功指标

### 15.1 内容质量

| 指标 | 目标 | 测量方式 |
|:---|:---|:---|
| 用户编辑率 | < 50% | 统计用户修改脚本的 clip 比例 |
| 用户保存率 | > 80% | 生成后用户保存/导出的比例 |
| 多版本复用率 | > 60% | 同一项目生成多语言/多平台版本的比例 |

### 15.2 效率提升

| 指标 | 目标 |
|:---|:---|
| 单项目处理时间 | < 10 分钟（不含渲染） |
| 人工审校时间 | < 15 分钟 / 项目 |
| 相比纯人工剪辑 | 节省 80% 以上时间 |

### 15.3 用户粘性

| 指标 | 目标 |
|:---|:---|
| 周活跃用户 | 待定 |
| 单用户月均项目数 | > 3 |
| NPS | > 40 |

---

## 16. 风险与假设

### 16.1 风险

| 风险 | 影响 | 缓解措施 |
|:---|:---|:---|
| MiniMax M3 对长文本/多模态理解不稳定 | 输出质量差 | 先做文字稿驱动，多模态作为辅助 |
| 声音克隆效果不达预期 | 多语言版本不可用 | 默认使用通用 TTS，克隆作为增值 |
| 视频渲染成本高/慢 | 用户体验差 | P0 先用图片轮播+字幕，不做复杂渲染 |
| 版权问题（音乐/图片） | 法律风险 | 使用免版权素材，明确用户责任 |
| 用户不会写口吻设定 | 输出不符合预期 | 提供默认模板和预览功能 |

### 16.2 假设

- 用户上传的素材音质/画质可接受
- 演讲内容有明确可传播的观点
- 用户愿意花 5-10 分钟做最终审校
- MiniMax M3 在 512K 上下文内稳定输出结构化 JSON

---

## 17. 路线图

详见 [SCHEDULE.md](./SCHEDULE.md) 的详细排期。

### Phase 1：P0 MVP（6 周）

**目标**：作为内部验证工具，跑通"上传 → AI 生成脚本 → 简单渲染 → 人工审校 → 导出"核心闭环。

**技术栈**：FastAPI + MiniMax M3 + 手搓 Agent + TanStack Start + PostgreSQL + 本地存储。

**包含**：
- Speaker Profile 创建 + 风格画像
- 文字稿 + 图片上传
- MiniMax M3 生成 3 个 clip 脚本 + LinkedIn 长帖 + 金句卡
- 人工审校 + 反馈重生成
- 简单视频渲染（图片轮播 + 字幕）
- 导出视频、文案、金句卡

**不包含**：自动转写、声音克隆、多语言配音、复杂剪辑、社媒发布。

### Phase 2：P1（6-8 周）

**目标**：产品化准备，扩展输入和输出能力。

- 视频/音频上传 + 自动转写
- 幻灯片解析
- 声音样本上传 + 克隆
- 多语言版本 + 通用 TTS
- 更多字幕样式、B-roll、版式切换
- 品牌套件
- 批量导出
- 对象存储迁移

### Phase 3：P2（8-12 周）

**目标**：SaaS 化。

- 用户体系与权限
- 计费系统
- 多租户
- 直接发布到社媒平台
- AI 生成动态 B-roll
- 数据分析
- 更多平台版式

---

## 18. 术语表

| 术语 | 说明 |
|:---|:---|
| Repurposing | 将已有内容重新加工，适配不同平台和形式 |
| Speaker Profile | 演讲者档案，包含风格画像、口吻设置、声音样本 |
| Clip | 生成的短视频片段 |
| Hook | 视频开头吸引注意力的句子 |
| Carousel | 多页图文，常见于 LinkedIn/Instagram |
| SRT | 字幕文件格式 |
| Persona | 人物风格画像 |
| B-roll | 辅助画面素材 |

---

## 19. 待决策事项

| 事项 | 建议 | 决策者 |
|:---|:---|:---|
| 产品名称 | 待定 | 左总 |
| Agent 框架选型 | **已决策：P0 手搓** | 技术 |
| 是否支持 URL 输入 | 本期不做，后续可作为补充 | 产品/技术 |
| 首期支持语言 | 中英双语 | 产品 |
| 是否接入社媒发布 API | P2 再考虑 | 产品 |
| 付费模式 | 本期不设计 | 左总 |
| 声音克隆服务选型 | P1 阶段评估 | 技术 |

---

## 20. Agent 框架选型决策

### 20.1 为什么需要 Agent 流程

用户说"不满意"通常不是整体重做，而是局部问题：

| 不满意类型 | 处理方式 |
|:---|:---|
| 整体风格不像本人 | 回到 Speaker Profile 重新校准 |
| 某个 clip 不够抓人 | 只重新生成这个 clip |
| Hook 不行 | 只重新生成标题/Hook |
| 翻译不自然 | 只重新翻译这段 |
| 画面和字幕不匹配 | 只换画面提示或 B-roll |
| 某个金句不够有力 | 只重新生成金句卡 |

因此系统不能是"一次生成就结束"，必须是：**生成初稿 → 用户反馈/自评 → 定位问题 → 局部重生成 → 用户确认**。

### 20.2 候选方案

| 框架 | 核心定位 | 适合本项目程度 |
|:---|:---|:---|
| **Pydantic AI** | 类型安全的 LLM Agent | 高，但需写 MiniMax Custom Model |
| **LangGraph** | 复杂状态机工作流 | 高，HITL 最强，但学习曲线陡 |
| **ControlFlow** | 结构化 Agent 任务流 | 高，代码最清晰，但生态小 |
| **CrewAI** | 角色扮演型多 Agent | 中，API 简单但控制力弱 |
| **dspy** | LLM 程序优化 | 中，适合持续优化 prompt |
| **手搓** | 自研 Agent 编排器 | 高，完全可控 |

### 20.3 手搓 vs Pydantic AI 对比

| 维度 | 手搓 | Pydantic AI |
|:---|:---|:---|
| 开发速度（P0） | 快，按业务直接写 | 前期稍慢，要写 MiniMax adapter |
| 学习成本 | 低 | 中 |
| 类型安全 | 自己用 Pydantic 校验 | 框架内置，更优雅 |
| MiniMax 兼容性 | 直接调用，零适配 | 需 Custom Model adapter |
| 调试/可观测性 | 白盒 | 黑盒一些 |
| 模块解耦 | 完全可控 | 受框架限制 |
| 未来扩展 | 代码可能变乱 | 更整洁 |
| 框架风险 | 无 | 新框架，可能有坑 |

### 20.4 决策结论

**P0 采用"手搓 Agent 工作流"方案。**

理由：
1. 项目场景明确，单模型（MiniMax M3），不需要框架的 provider 抽象
2. Workflow 明确（persona → analyze → script → review → revise → HITL），不是开放式 Agent
3. Prompt 需要精细控制，框架模板可能不够灵活
4. MiniMax M3 需要适配，框架优势被削弱
5. 团队熟悉 FastAPI/Pydantic，手搓就是把这些能力组合起来
6. P0 的核心是验证"内容生成质量"，不是验证"Agent 框架能力"

**未来**：如果 P1/P2 workflow 复杂到代码难以维护，再评估迁移到 Pydantic AI 或 LangGraph。

### 20.5 手搓架构建议

```
speech-repurposer/
├── api/                    # FastAPI 路由
│   └── routes.py
├── core/                   # 业务核心
│   ├── workflow.py         # Workflow 编排器
│   ├── agents/             # Agent steps
│   │   ├── persona.py
│   │   ├── analyzer.py
│   │   ├── script.py
│   │   ├── reviewer.py
│   │   ├── reviser.py
│   │   ├── linkedin.py
│   │   └── quote_card.py
│   └── prompts/            # Jinja2 模板
│       ├── persona.j2
│       ├── script.j2
│       ├── review.j2
│       └── revise.j2
├── clients/
│   └── minimax.py          # MiniMax M3 封装
├── models/
│   └── schemas.py          # Pydantic 模型
├── state/
│   └── store.py            # 数据库/状态管理
└── main.py
```

### 20.6 关键设计点

1. **每个 Agent step 都是纯函数**：输入 context + prompt，输出 Pydantic model
2. **统一 MiniMax client**：封装调用、JSON 清洗、重试、错误处理
3. **Workflow 记录每步状态**：便于调试和 HITL 恢复
4. **用户反馈结构化**：不是自由文本，而是选择原因 + 可选补充说明
5. **版本历史**：每次重生成保存旧版本，支持 A/B 对比和回滚

### 20.7 依赖库（非框架）

手搓不代表从零开始，可以使用这些库：

| 用途 | 库 |
|:---|:---|
| HTTP 调用 | httpx |
| 数据校验 | pydantic |
| 重试机制 | tenacity |
| Prompt 模板 | jinja2 |
| 异步任务 | asyncio / 任务队列 |
| 日志 | structlog / logging |
| 数据库 | SQLAlchemy 等 |

---
