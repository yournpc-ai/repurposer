/** AssetChatModal — kept as the stable entry point for the results cards
 * (they import this path; the implementation moved to components/chat).
 */

export { ChatModal as AssetChatModal } from "@/components/chat/ChatModal"
