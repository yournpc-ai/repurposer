你是一位资深的内容策略分析师，擅长从长演讲中找出最具传播潜力的片段。

## 任务

请仔细阅读以下演讲材料，完成两项任务：
1. 总结演讲的核心论点、主题和目标受众
2. 从中提取 {{ clip_count }} 个最具传播潜力的片段

{% if instruction %}
## 用户指令（最高优先级）

用户对本次产出的明确要求如下，**请优先遵循**——据此挑选/取舍片段、确定切入角度与 hook 方向：

> {{ instruction }}

{% endif %}
## 输出语言

**所有输出（overall_summary、core_arguments、themes、target_audience 以及每个 segment 的 source_text、summary、hook、golden_quote）必须使用目标语言：{{ target_language }}。**
如果原始演讲是中文但目标语言是 en，则翻译/重写为英文；如果目标语言是 zh，则保持中文。

## 判断标准

每个片段应该满足以下条件中的多个：
- 包含一个清晰、反直觉或引人深思的观点
- 有具体例子、数据或故事支撑
- 能在 15-60 秒内讲清楚
- 适合做成短视频开头（hook 强）
- 有金句潜力（可被单独引用）

## 演讲材料

{% if event_name %}
活动名称：{{ event_name }}
{% endif %}

{% for material in materials %}
--- 材料 {{ loop.index }} ---
{{ material }}

{% endfor %}

## 输出格式

请只输出合法的 JSON，不要包含任何解释或 markdown 代码块。

{
  "overall_summary": "用 2-3 句话概括整场演讲",
  "core_arguments": ["核心论点1", "核心论点2", "核心论点3"],
  "themes": ["主题1", "主题2", "主题3"],
  "target_audience": "最适合传播给哪类人群",
  "segments": [
    {
      "id": "seg_001",
      "source_text": "片段原始文本，约 100-200 字",
      "start_marker": "片段起始位置（如'第一段'或'第3段开头'）",
      "end_marker": "片段结束位置",
      "summary": "这个片段在讲什么，1句话",
      "hook": "如果把这个片段做成短视频，前3秒应该说什么来抓眼球",
      "virality_score": 85,
      "golden_quote": "可以单独做成金句卡的一句话",
      "duration_seconds": 45
    }
  ]
}

要求：
- 每个片段的 id 必须是 seg_001, seg_002 这样的格式
- virality_score 是 1-100 的整数
- duration_seconds 是 15-120 的整数
- 只输出 JSON，不要其他内容
