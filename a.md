# 本周工作

### 1. 进一步收敛产品与技术的五层架构

首先完成了系统整体架构的进一步收敛，明确五个核心层级：

1. **Agent / Tool 层**：理解用户、做判断、调用工具。
2. **Workflow / Execution Kernel 层**：DAG、Run/Step 生命周期、HITL、可靠执行。
3. **Artifact 层**：当前以 Output、操作账本、恢复机制等现有能力支撑，统一不可变 Artifact 模型保持 OPEN。
4. **Structured Media 层**：以 ClipSpec、轨道模型等承载结构化媒体生产。
5. **Decompiler / Compiler 层**：参考视频拆解，以及结构化内容到最终渲染产物。



目标：

> **DAG + 状态机只是执行层，整个产品方向是 Agentic Content OS。**


---

### 2. 处理产物生产边界情况

重点解决的是：

> **任务一旦进入内容生产执行阶段，系统不能因为并发、失败、僵尸 Worker 等问题产生错误副作用。**

包括：

* 防止重复执行；
* 防止重复扣费；
* 防止旧 Worker 继续写入；
* 防止任务无限失败循环；
* 保证挂起 / 恢复和多轮执行的一致性。

---


### 3. Chat 升级：从“机械执行”到“证明我真的理解了”

真实产品测试暴露出一类问题：

系统内部其实在正常工作，但用户看到的状态可能：

* 像卡住；
* 像出错；
* 两个 Agent 同时说话；
* 中途状态和最终结果不一致；
* 系统失败后用户输入甚至可能丢失。

进一步推进：

> **让 Agent 的内部状态、用户看到的状态、最终结果保持一致。**

目标体验：

> 用户说一句话 → 系统明确告诉他正在做什么 → 中间遇到问题能够自己恢复或明确求助 → 最后一定有清楚的结果。

同时解决 **多个 Agent 在同一个用户回合里抢麦**的问题。

当前 Chat 已经可以理解用户需求、调用 Tool、生成计划，但产品体验仍然偏机械。

进一步明确 Chat 的产品职责：

> **素材理解 → Agent 判断 → 用户意图确认 → 生产计划**

尤其在用户确认、可能产生成本之前，要让用户先确认：

> **Agent 真的看懂了我的素材。**

因此 Chat 不只是告诉用户：

> “你想做什么，我准备怎么做。”

还要能够告诉用户：

> “我看到了什么、我理解的核心是什么，以及为什么这个计划适合你的内容。”

---

### 4. Canvas 升级：让用户真正看懂生产计划

Canvas 从单纯展示内部 Graph，进一步转向：

> **帮助用户理解“我让你做什么，你准备怎么做”。**

本周重点包括：

* 节点布局升级；
* 节点信息表达升级；
* 节点关系与视觉组织升级；
* Agent 操作路径的可理解性；
* 让生产链条更容易阅读。

产品职责进一步明确：

> **Chat 负责 理解/判断/确认/汇报；Canvas 负责 结构/计划/产物。**

目标不是让画布“更漂亮”，而是让用户能够快速理解 agent：

> **输入是什么 → 要做什么 → 怎么做 → 会得到什么。**

---

### 5. 深化 Agent / Tool：把参考视频变成可编程的生产结构

继续聚焦 Agent / Tool 层，推进参考视频仿制能力。

目标从：

> “Agent 能看懂一个参考视频”

进一步走向：

> **把参考视频拆成真正能够进入系统的结构化、可编程时序数据。**

也就是让系统能够理解并沉淀：

* 时间结构；
* Clip / Segment；
* 字幕；
* 节奏；
* 音乐；
* 转场；
* 视觉结构等。

这为未来更强的 Remix / Recomposition 能力提供基础，但当前仍然以产品证据驱动，不提前承诺统一 Media IR。

---

### 6. 清理架构与文档：把系统重新变得“容易理解”

最后，现在项目有点大了，对现有文档和历史决策进行系统清理，进入一个 coding session 时需要加载多少上下文、哪些规则是真规则、哪些只是历史考古：

* 删除已经过时的设计；
* 清理已被否决的路线；
* 区分当前事实、North Star、OPEN 假设和已排期事项；
* 减少历史上下文和重复决策；
* 保证下一次开发看到的是当前真实系统。

目标：

> **让代码、架构、产品和文档重新保持同一个方向，降低后续开发和决策成本。**

---

## 总结

> **这周我们完成了视频生产可靠和五层整体架构收敛，并开始把产品从“Agent 能完成任务”推进到“Agent 能理解用户、证明自己理解、解释生产计划，并以用户能看懂的方式完成任务”。**

